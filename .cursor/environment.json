{
  "name": "Splat",
  "install": "npm ci && ([ -f .env ] || cp .env.example .env)",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Vite dev server for the Splat SPA on http://localhost:8080"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8080
    }
  ]
}
