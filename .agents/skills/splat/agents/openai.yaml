interface:
  display_name: "Splat"
  short_description: "Repo-specific patterns and workflows for splat"
  default_prompt: "Use the splat repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true